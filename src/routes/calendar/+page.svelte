<!-- <script lang="ts">
	import { DateContext, type DateData } from '../../stores/dateContext';
	import { DataContext, type PageData } from '../../stores/dataContext';
	import Calender from '../../components/WorkerConfig.svelte';

	let dateData: DateData;
	let pageData: PageData;

	DateContext.subscribe((value) => {
		dateData = value;
	});

	DataContext.subscribe((value) => {
		pageData = value;
	});
</script>

<Calender dateContext={dateData} dataContext={pageData} /> -->
